export enum CARD_TEMPLATE {
  TEMPLATE_ID = 'card',
  TITLE = ".title",
  TEXT = ".text",
  TODO = "todo",
  PROGRESS = "progress",
  DONE = "done",
}

export enum TARGET_SLOT {
  TODO = "todo-slot",
  PROGRESS = "progress-slot",
  DONE = "done-slot",
}
